import React, { Component } from 'react';
import PropTypes from 'prop-types';

class StorySection extends Component {

	render () {
		return(
			this.props.storySection.arrayIndex
		)
	}
	
}

export default StorySection;